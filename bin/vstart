#!/bin/bash

for i in "$@"
do
    if [ "$i" = "${@: -1}" ]; then 
        NETKIT_LASTARG=${@: -1}
        shift
    fi
done

$NETKIT_HOME/lstart -d python $NETKIT_HOME/python/vstart.py "$PWD/" $NETKIT_LASTARG $@

rm -rf "$HOME/netkit_temp/netkit_${UID}_${NETKIT_LASTARG}"