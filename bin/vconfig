#!/bin/bash

for i in "$@"
do
    if [ "$i" = "${@: -1}" ]; then shift
    fi
done

$NETKIT_HOME/lstart -d `python $NETKIT_HOME/python/vstart.py "$PWD/" ${@: -1} $@`

rm -rf "$HOME/netkit_temp/netkit_${UID}_${@: -1}"
